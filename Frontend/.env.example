# Copy to .env.local for local Vite, or set in Vercel Project → Settings → Environment Variables.
# After deploying the backend on Render, put that public URL here (no trailing slash).
# Example: https://ushirika-api.onrender.com
VITE_API_URL=http://localhost:8000
